@page "/testupload"
@rendermode InteractiveServer
@inject ILogger<Test> Logger

<h1>Test Upload Page</h1>


<InputFile OnChange="@UploadFile" multiple />

@code {
    // This will execute when the component is initialized (page loads)
    protected override void OnInitialized()
    {
        Logger.LogInformation("Hello From TestUpload");
    }

    private async Task UploadFile(InputFileChangeEventArgs e)
    {
        Logger.LogInformation("UploadFile method triggered!"); // Keep your existing log here too
    // ... rest of your upload logic ...
    }
}