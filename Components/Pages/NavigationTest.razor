@page "/navigate-demo"
@inject NavigationManager NavigationManager

<button @onclick="HandleNavigation">Go to Counter</button>

@code {

    private void HandleNavigation()
    {
    // Do any processing needed before navigation
    // For example, validate or save data

    // Then navigate to the new page
        NavigationManager.NavigateTo("/");
    }

}